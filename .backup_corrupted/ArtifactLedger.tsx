"import React, { useMemo } from 'react';\nimport { motion } from 'framer-motion';\n\ninterface Turn {\n  id: number;\n  responses: any | null;\n}\n\ninterface ArtifactLedgerProps {\n  turns: Turn[];\n  onClose: () => void;\n}\n\nfunction extractTitle(html: string): string {\n  const match = html.match(/<title>(.*?)<\\/title>/i);\n  return match?.[1]?.trim() || \"Untitled Artifact\";\n}\n\nexport function ArtifactLedger({ turns, onClose }: ArtifactLedgerProps) {\n  const artifacts = useMemo(() => {\n    const list: { id: string; html: string; title: string; turnId: number }[] = [];\n    let counter = 0;\n    const htmlSet = new Set<string>();\n\n    turns.forEach(turn => {\n      if (!turn.responses) return;\n      Object.entries(turn.responses).forEach(([model, text]) => {\n        if (typeof text !== 'string') return;\n        // Match both ```html and ```iframe\n        const regex = /```(?:html|iframe)\\n([\\s\\S]*?)```/g;\n        let match;\n        while ((match = regex.exec(text)) !== null) {\n          const code = match[1].trim();\n          if (!htmlSet.has(code)) {\n            htmlSet.add(code);\n            list.push({\n              id: `art-${turn.id}-${model}-${counter++}`,\n              html: code,\n              title: extractTitle(code),\n              turnId: turn.id\n            });\n          }\n        }\n      });\n    });\n    // Reverse to show the newest artifacts at the top\n    return list.reverse();\n  }, [turns]);\n\n  return (\n    <div className=\"flex flex-col h-full bg-[#0D1117] text-white\">\n      {/* Header */}\n      <div className=\"flex items-center justify-between px-4 py-3 border-b border-white/[0.06]\">\n        <div className=\"flex items-center gap-2\">\n          <svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" strokeLinecap=\"round\" strokeLinejoin=\"round\" className=\"text-purple-400\">\n            <rect x=\"3\" y=\"3\" width=\"18\" height=\"18\" rx=\"2\" ry=\"2\"><
<truncated 3161 bytes>