"import React, { memo } from 'react';\nimport type { FairValue, Team } from '../../types/matchup.types';\n\n// ============================================================================\n// FairValueCard — Devigged win probabilities + fair odds\n// ============================================================================\n// The side with the higher probability gets .favorite class,\n// the lower gets .underdog. No generic green/red colors.\n// ============================================================================\n\ninterface FairValueCardProps {\n  fairValue: FairValue;\n  awayTeam: Team;\n  homeTeam: Team;\n}\n\nfunction formatOdds(odds: number): string {\n  return odds > 0 ? `+${odds}` : `${odds}`;\n}\n\nexport const FairValueCard = memo<FairValueCardProps>(({ fairValue, awayTeam, homeTeam }) => {\n  const awayIsFav = fairValue.awayWinPct >= fairValue.homeWinPct;\n\n  return (\n    <div className=\"mu-card\" style={{ marginBottom: 20 }}>\n      <div className=\"mu-label-md\" style={{ marginBottom: 16, textAlign: 'center' }}>\n        Fair Value\n      </div>\n\n      <div className=\"mu-fv\">\n        {/* Away side */}\n        <div className={`mu-fv-side ${awayIsFav ? 'favorite' : 'underdog'}`}>\n          <div className=\"mu-label\" style={{ marginBottom: 4 }}>{awayTeam.abbr}</div>\n          <div className=\"mu-fv-pct\">{fairValue.awayWinPct.toFixed(1)}%</div>\n          <div className=\"mu-fv-odds\">{formatOdds(fairValue.awayFairOdds)}</div>\n          <div className=\"mu-fv-market\">Mkt {formatOdds(fairValue.marketAwayOdds)}</div>\n        </div>\n\n        <div className=\"mu-fv-divider\" />\n\n        {/* Home side */}\n        <div className={`mu-fv-side ${!awayIsFav ? 'favorite' : 'underdog'}`}>\n          <div className=\"mu-label\" style={{ marginBottom: 4 }}>{homeTeam.abbr}</div>\n          <div className=\"mu-fv-pct\">{fairValue.homeWinPct.toFixed(1)}%</div>\n          <div className=\"mu-fv-odds\">{formatOdds(fairValue.homeFairOdds)}</div>\n          <div className=\"mu-fv-market\">
<truncated 152 bytes>