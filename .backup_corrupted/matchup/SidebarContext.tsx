"import React, { memo } from 'react';\nimport type { Team, Venue, HeadToHead } from '../../types/matchup.types';\n\n// ============================================================================\n// SidebarContext — Form, Season Series, and Venue cards\n// ============================================================================\n// Series progress bar width is dynamically calculated:\n// (homeWins / totalGames) * 100%\n// ============================================================================\n\ninterface SidebarContextProps {\n  awayTeam: Team;\n  homeTeam: Team;\n  headToHead: HeadToHead;\n  last10: { away: string; home: string };\n  runDiff: { away: string; home: string };\n  venue: Venue;\n}\n\nexport const SidebarContext = memo<SidebarContextProps>(({\n  awayTeam,\n  homeTeam,\n  headToHead,\n  last10,\n  runDiff,\n  venue,\n}) => {\n  const totalGames = headToHead.awayWins + headToHead.homeWins;\n  const homeSeriesPct = totalGames > 0 ? (headToHead.homeWins / totalGames) * 100 : 50;\n  const awaySeriesPct = 100 - homeSeriesPct;\n\n  return (\n    <div className=\"mu-sidebar\">\n      {/* ── Form (Last 10) ────────────────────────────────────────────── */}\n      <div className=\"mu-card\">\n        <div className=\"mu-card-title\">Form · Last 10</div>\n        <div className=\"mu-form-row\">\n          <span className=\"mu-form-team\">{awayTeam.abbr}</span>\n          <span className=\"mu-form-val\">{last10.away}</span>\n        </div>\n        <div className=\"mu-form-row\">\n          <span className=\"mu-form-team\">{homeTeam.abbr}</span>\n          <span className=\"mu-form-val\">{last10.home}</span>\n        </div>\n      </div>\n\n      {/* ── Run Differential ──────────────────────────────────────────── */}\n      <div className=\"mu-card\">\n        <div className=\"mu-card-title\">Run Differential</di
<truncated 2428 bytes>