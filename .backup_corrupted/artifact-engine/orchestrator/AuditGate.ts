"import { AgentResult } from \"../contracts/tasks\";\nimport { PatchSet } from \"../contracts/patch\";\nimport { validateBlockData } from \"../contracts/schemas\";\nimport { log } from \"../obs/log\";\nimport { metrics, METRIC } from \"../obs/metrics\";\n\n// ============================================================================\n// Audit Gate — Production-Hardened Provenance Enforcement\n// ============================================================================\n// Filters out:\n// 1. Failed agent results\n// 2. Upserts with zero-row provenance (kills fabrication)\n// 3. Stale provenance (freshness TTL)\n// 4. Unknown/disallowed data sources (source allowlist)\n// 5. Structurally invalid block data (schema validation)\n//\n// Exception: \"empty_state\" blocks are allowed with 0 rows (they render\n// an explicit \"no data available\" message).\n// ============================================================================\n\n// ── Audit configuration ─────────────────────────────────────────────────────\n\nconst AUDIT_CONFIG = {\n  /** Maximum age of provenance data before it's considered stale (2 hours) */\n  maxProvenanceAgeMs: 2 * 60 * 60 * 1000,\n\n  /** Allowed source prefixes — any block with a source not matching these is rejected */\n  sourceAllowlistPrefixes: [\"spanner:\", \"bigquery:\", \"api:\", \"cache:\"],\n};\n\nexport class AuditGate {\n  /**\n   * Filter agent results: drop failures, reject blocks that fail any audit check.\n   * Returns only the patchsets that pass all gates.\n   */\n  static filter(results: AgentResult[]): PatchSet[] {\n    const out: PatchSet[] = [];\n\n    for (const r of results) {\n      // Skip failed agents entirely\n      if (!r.ok || !r.patchset) continue;\n\n      // Filter individual patches within the patchset\n      const safePatches = r.patchset.patches.filter((p) => {\n        // Noops and removes are always safe\n        if (p.op !== \"ups
<truncated 3323 bytes>