"import { describe, it, expect, beforeEach } from \"vitest\";\nimport { AuditGate } from \"../orchestrator/AuditGate\";\nimport { AgentResult } from \"../contracts/tasks\";\nimport { Block, Provenance } from \"../contracts/artifact\";\nimport { metrics } from \"../obs/metrics\";\n\n// ============================================================================\n// AuditGate Tests — 5-Gate Enforcement\n// ============================================================================\n// Validates every gate in isolation and in combination:\n// 1. Zero-row fabrication\n// 2. Low confidence\n// 3. Provenance freshness (TTL)\n// 4. Source allowlist\n// 5. Schema validation\n// ============================================================================\n\n// ── Test Helpers ────────────────────────────────────────────────────────────\n\nfunction makeProv(overrides: Partial<Provenance> = {}): Provenance {\n  return {\n    source: \"spanner:TestTable\",\n    fetched_at: new Date().toISOString(),\n    row_count: 4,\n    confidence: \"high\",\n    ...overrides,\n  };\n}\n\nfunction makeBlock(overrides: Partial<Block> = {}): Block {\n  return {\n    id: \"test.block.1\",\n    type: \"standings_table\",\n    domain: \"standings\",\n    version: 1,\n    data: {\n      group: \"A\",\n      table: [{ code: \"GER\", mp: 3, w: 2, d: 1, l: 0, gf: 7, ga: 2, gd: 5, pts: 7 }],\n    },\n    provenance: makeProv(),\n    slot: \"group.A\",\n    order: 0,\n    hash: \"abcdef1234567890\",\n    ...overrides,\n  };\n}\n\nfunction makeResult(block: Block, ok = true): AgentResult {\n  return {\n    task_id: \"test-task-1\",\n    domain: block.domain,\n    ok,\n    patchset: {\n      artifact_id: \"test-artifact\",\n      base_rev: 0,\n      domain: block.domain,\n      patches: [{ op: \"upsert\", block }],\n    },\n    metrics: { ms: 10, rows_read: block.provenance.row_count, blocks_changed: 1 },\n  };\n}\n\nbeforeEach
<truncated 6961 bytes>